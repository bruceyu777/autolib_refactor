<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FortiOS Release QA Automation</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/themes/prism.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/plugins/line-numbers/prism-line-numbers.min.css" rel="stylesheet" />
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }
        .header {
            background-color: #4CAF50;
            color: white;
            padding: 10px;
            font-size: 1.5em;
        }
        .breadcrumb {
            margin: 10px 0;
            font-size: 1.2em;
            color: #333;
        }
        .breadcrumb a {
            color: #4CAF50;
            text-decoration: none;
        }
        .breadcrumb a:hover {
            text-decoration: underline;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border-top: 1px solid #ddd;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
            padding: 10px;
            text-align: left;
            font-weight: bold;
        }
        td {
            padding: 10px;
        }
        .file-icon {
            display: inline-block;
            width: 16px;
            height: 16px;
            margin-right: 10px;
            vertical-align: middle;
        }
        .file-icon.folder {
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" fill="%230366d6" viewBox="0 0 24 24"><path d="M10 4H2v16h20V6H12L10 4zm0 2.5l2 2h8V18H4V6h6z"/></svg>') no-repeat center;
        }
        .file-icon.file {
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" fill="%23888" viewBox="0 0 24 24"><path d="M14 2H6c-1.1 0-2 .9-2 2v16c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V8l-6-6zm2 16H8v-2h8v2zm0-4H8v-2h8v2zm-3-5V3.5L18.5 9H13z"/></svg>') no-repeat center;
        }
        .file-size, .file-date {
            text-align: right;
        }
        .footer {
            background-color: #f2f2f2;
            padding: 10px;
            text-align: center;
            clear: both;
            position: fixed;
            bottom: 0;
            width: calc(100% - 80px); /* subtract 80px to account for the 40px margin on both sides */
            margin: 0 auto;
            box-sizing: border-box; /* include padding and border in width calculation */
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <div class="header">
        <span>File Browser</span>
    </div>

    <!-- Breadcrumb Section -->
    <div class="breadcrumb">
        {% for part, link in breadcrumbs %}
            / <a href="{{ link }}">{{ part }}</a>
        {% endfor %}
    </div>
    <div id="content">{% block content %}{% endblock %}</div>

</body>
<footer class="footer">
    <div class="container">
        <!-- <span class="text-muted">Place sticky footer content here.</span> -->
        <span class="text-muted"> &copy;
            <span id="copyright">
                <script>document.getElementById('copyright').appendChild(document.createTextNode(new Date().getFullYear()))</script>
            </span> Copyright:
            <a href=" https://www.fortinet.com/ "
                style=" text-decoration:none; color: inherit "><strong>www.fortinet.com</strong></a>
            All Rights Reserved.</span>
    </div>
</footer>
</html>
