# 1015663：Verify s02-license can be valided
# fortiguard settting default (anycast=enable)
# 2020-10-16
#############################

[PC_KVM]
    ### 1015663.txt ###
    comment: Verify s02 can be valided base on cpu cores with default FGD setting

    # get license file name
    #cat PC_KVM:LICENSE_DIR/PC_KVM:LIECENSE_FILE | grep 02S
    #setvar -e "(?n)^02S,(FGVMSL.*?lic)," -to lic
    setlicense -t 02S -for SN -to lic 

[FVM_A]
    
    # fortiguard default setting
    config system fortiguard
        unset fortiguard-anycast
    end
    
    execute restore vmlicense tftp FVM_A:LICENSE_DIR{$lic} FVM_A:LICENSE_SERVER
    sleep 60 
	
    get system status | grep 'Serial-Number' -A 3
    expect -e "FGVMSL" -for 1015663 -t 5
    expect -e "License Status: Valid" -for 1015663 -t 5
    expect -e "1 CPU/2 allowed" -for 1015663 -t 5

    #s-license need a reboot or exe cpu add to activate
    #exe cpu add cause kernel panic, comment it now
    #exe cpu add 1
    exe reboot
    y

    sleep 60
    forcelogin 
    exe cpu show
    expect -e "Active CPU number: 2" -for 1015663 -t 10

    diag debug vm-print-license
    expect -e "Model: SL" -for 1015663 -t 5
    expect -e "CPU: 2" -for 1015663 -t 5

    #diag test update info | grep 'AVDB'
    #expect -e "AVDB,(.*)2022" -for 1015663 -t 5

    report 1015663
   
